<%= form_for(@guest_house) do |f| %>
  <% if @guest_house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guest_house.errors.count, "error") %> prohibited this guest_house from being saved:</h2>

      <ul>
      <% @guest_house.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :city_id %><br>
    <%= f.number_field :city_id %>
  </div>
  <div class="field">
    <%= f.label :stars_count %><br>
    <%= f.number_field :stars_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
